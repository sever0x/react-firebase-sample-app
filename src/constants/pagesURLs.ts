import * as pages from './pages';

const result = {
    [pages.defaultPage]: `/${pages.defaultPage}`,
    [pages.create]: `/${pages.create}`,
    [pages.update]: `/${pages.update}`,
}

export default result;